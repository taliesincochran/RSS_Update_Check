# RSS feed update check
To run this program, run the following in git bash: 
````
$ git clone git@github.com:taliesincochran/RSS_Update_Check.git
  npm install
  npm start <path to dictionary> <number of days> 
````
